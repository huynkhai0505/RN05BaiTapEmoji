import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    root: {
        flex: 1,
        backgroundColor: '#E9EBEE',
        justifyContent: 'center',
        alignItems: 'center'
    },
    container_choice: {
        flexDirection: 'row',
    },
    container_choice__image: {
        height: 40,
        width: 40,
        margin: 10,
    },
    text: {
        fontWeight: 'bold',
        fontSize: 20
    }

})